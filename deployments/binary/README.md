
启动服务前，请将配置文件复制到 configs 目录，并准备好二进制文件。

```
├── configs
│         └── serverNameExample.yml
├── serverNameExample
├── deploy.sh
└── run.sh
```

### 手动运行与停止服务

运行服务：

> ./run.sh

停止服务：

> ./run.sh stop

<br>

### 自动化部署服务

> ./deploy.sh
